<Window
    x:Class="BlueMeter.WPF.Views.DpsStatisticsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:behaviors="clr-namespace:BlueMeter.WPF.Behaviors"
    xmlns:converters="clr-namespace:BlueMeter.WPF.Converters"
    xmlns:ctrl="clr-namespace:BlueMeter.WPF.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:loc="clr-namespace:BlueMeter.WPF.Properties"
    xmlns:local="clr-namespace:BlueMeter.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:BlueMeter.Core.Models;assembly=BlueMeter.Core"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:viewModels="clr-namespace:BlueMeter.WPF.ViewModels"
    xmlns:wpfModels="clr-namespace:BlueMeter.WPF.Models"
    Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Window_DpsStatistics_Title}}"
    Width="380"
    Height="360"
    MinWidth="300"
    MinHeight="35"
    behaviors:WindowSnapBehavior.PreventSnapMaximize="True"
    d:DataContext="{d:DesignInstance Type=viewModels:DpsStatisticsDesignTimeViewModel,
                                     IsDesignTimeCreatable=True}"
    lex:ResxLocalizationProvider.DefaultAssembly="BlueMeter.WPF"
    lex:ResxLocalizationProvider.DefaultDictionary="Resources"
    ResizeMode="CanResize"
    Style="{StaticResource TransparentWindow}"
    TextOptions.TextFormattingMode="Display"
    TextOptions.TextRenderingMode="ClearType"
    Topmost="{Binding AppConfig.TopmostEnabled, Mode=OneWay}"
    WindowStartupLocation="Manual"
    mc:Ignorable="d">

    <Window.Opacity>
        <Binding Path="AppConfig.Opacity" Converter="{StaticResource PercentToOpacityConverter}" />
    </Window.Opacity>

    <!--  WindowChrome: 开启可拉伸边界  -->
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            GlassFrameThickness="0"
            ResizeBorderThickness="6"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </b:EventTrigger>
        <b:EventTrigger EventName="Unloaded">
            <b:InvokeCommandAction Command="{Binding UnloadedCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Window.Resources>
        <converters:DoubleNegateConverter x:Key="DoubleNegateConverter" />
        <converters:DoubleSumConverter x:Key="DoubleSumConverter" />
        <converters:BackgroundImageConverter x:Key="BackgroundImageConverter" />
        <converters:ThemeColorConverter x:Key="ThemeColorConverter" />
        <converters:ThemeColorOverlayConverter x:Key="ThemeColorOverlayConverter" />
        <converters:ThemeBackgroundConverter x:Key="ThemeBackgroundConverter" />
        <converters:ThemeAccentConverter x:Key="ThemeAccentConverter" />
        <converters:ThemeForegroundConverter x:Key="ThemeForegroundConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:PanelColorModeConverter x:Key="PanelColorModeConverter" />
        <converters:PanelContentColorModeConverter x:Key="PanelContentColorModeConverter" />
        <converters:PercentToOpacityConverter x:Key="PercentToOpacityConverter" />
        <converters:TrainingModeCheckConverter x:Key="TrainingModeCheckConverter" />

        <!-- DPS Settings MenuItem with black text for readability on light background -->
        <Style x:Key="DpsSettingsMenuItem" TargetType="MenuItem" BasedOn="{StaticResource ModernMenuItem}">
            <Setter Property="Foreground" Value="#000000" />
        </Style>

        <!-- DPS Settings CheckableMenuItem with black text -->
        <Style x:Key="DpsCheckableMenuItem" TargetType="MenuItem" BasedOn="{StaticResource CheckableMenuItem}">
            <Setter Property="Foreground" Value="#000000" />
        </Style>

        <Style x:Key="StatisticTypeText" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeForegroundConverter}, ConverterParameter=text}" />
            <Setter Property="FontFamily" Value="pack://application:,,,/Assets/Fonts/#HarmonyOS Sans SC" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>

        <Style BasedOn="{StaticResource IconButtonStyle}" TargetType="Button">
            <Setter Property="FontFamily" Value="pack://application:,,,/Assets/Fonts/#iconfont" />
            <Setter Property="Background" Value="{x:Null}" />
            <Setter Property="Foreground" Value="White" />
        </Style>

        <Style x:Key="LoadingBlockRectangle" TargetType="Rectangle">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Width" Value="150" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FooterContentBorder" TargetType="Border">
            <Setter Property="VerticalAlignment" Value="Bottom" />
            <Setter Property="Height" Value="25" />
            <Setter Property="Opacity" Value="1" />
            <Setter Property="CornerRadius">
                <Setter.Value>
                    <CornerRadius
                        BottomLeft="{StaticResource MainCornerRadiusDouble}"
                        BottomRight="{StaticResource MainCornerRadiusDouble}"
                        TopLeft="0"
                        TopRight="0" />
                </Setter.Value>
            </Setter>

            <EventSetter Event="MouseLeftButtonDown" Handler="TitleBar_MouseLeftButtonDown" />

            <Style.Triggers>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <Binding
                            Converter="{StaticResource LessThanToBoolConverter}"
                            ConverterParameter="60"
                            Path="ActualHeight"
                            RelativeSource="{RelativeSource AncestorType=local:DpsStatisticsView}" />
                    </DataTrigger.Binding>
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                                    Storyboard.TargetProperty="Opacity"
                                    To="0"
                                    Duration="0:0:0.2" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                                    Storyboard.TargetProperty="Opacity"
                                    To="1"
                                    Duration="0:0:0.2" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="StatisticDetailDataTemplate" DataType="{x:Type viewModels:StatisticDataViewModel}">
            <Grid HorizontalAlignment="Stretch">
                <!--  名次  -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="A" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="B" />
                    <ColumnDefinition Width="*" SharedSizeGroup="C" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="D" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="E" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Margin="4,0,4,0"
                    VerticalAlignment="Center"
                    FontFamily="pack://application:,,,/Assets/Fonts/#SAO Welcome TT"
                    Foreground="White">
                    <TextBlock.Text>
                        <Binding
                            Path="Index"
                            StringFormat="{}00"
                            UpdateSourceTrigger="PropertyChanged" />
                    </TextBlock.Text>
                </TextBlock>

                <!--  职业图标  -->
                <!--  Width="{Binding ActualHeight, ElementName=PlayerName}"  -->
                <!--  Height="{Binding ActualHeight, ElementName=PlayerName}"  -->
                <Image
                    Grid.Column="1"
                    Width="22"
                    Height="22"
                    Margin="2"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    SnapsToDevicePixels="True"
                    Source="{Binding Player.Class, Converter={StaticResource ClassesToIconConverter}, FallbackValue={StaticResource ClassesUnknownIcon}}"
                    Stretch="Uniform"
                    UseLayoutRounding="True" />
                <!--  昵称  -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style x:Key="PlayerInfoTextStyle" TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="FontFamily" Value="pack://application:,,,/Assets/Fonts/#HarmonyOS Sans SC" />
                            <Setter Property="Foreground" Value="White" />
                        </Style>
                    </StackPanel.Resources>
                    <TextBlock
                        x:Name="PlayerName"
                        Style="{StaticResource PlayerInfoTextStyle}"
                        Text="{Binding Player.Name}"
                        Visibility="{Binding Player.IsNpc, Converter={StaticResource BoolToInvertedVisibilityConverter}}" />
                    <!--  Localized NPC label from Monster dictionary using UID (visible when NPC)  -->
                    <TextBlock
                        lex:ResxLocalizationProvider.DefaultDictionary="Monster"
                        Style="{StaticResource PlayerInfoTextStyle}"
                        Text="{lex:Loc {Binding Player.Uid}}"
                        Visibility="{Binding Player.IsNpc, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock Style="{StaticResource PlayerInfoTextStyle}" Visibility="{Binding Player.IsNpc, Converter={StaticResource BoolToInvertedVisibilityConverter}}">
                        <!--  Player name (visible when not NPC)  -->
                        <Run Text=" -" />
                        <Run Text="{lex:Loc {Binding Player.Spec, StringFormat=ClassSpec_{0}}}" />
                        <Run Text="{Binding Player.PowerLevel, StringFormat={}({0})}" />
                    </TextBlock>
                </StackPanel>

                <!--  数值  -->
                <TextBlock
                    Grid.Column="3"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    FontFamily="pack://application:,,,/Assets/Fonts/#HarmonyOS Sans SC"
                    FontWeight="Bold"
                    Foreground="White">
                    <Run>
                        <Run.Text>
                            <MultiBinding Converter="{StaticResource HumanReadableNumberConverter}">
                                <Binding Path="Value" />
                                <Binding Path="DataContext.AppConfig.DamageDisplayType" RelativeSource="{RelativeSource AncestorType=Window}" />
                            </MultiBinding>
                        </Run.Text>
                    </Run>
                    <Run>
                        <Run.Text>
                            <MultiBinding Converter="{StaticResource DpsConverter}" StringFormat="{}[{0}]">
                                <Binding Path="Value" />
                                <Binding Path="DataContext.BattleDuration" RelativeSource="{RelativeSource AncestorType=Window}" />
                                <Binding Path="DataContext.AppConfig.DamageDisplayType" RelativeSource="{RelativeSource AncestorType=Window}" />
                            </MultiBinding>
                        </Run.Text>
                    </Run>
                </TextBlock>
                <TextBlock
                    Grid.Column="4"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    FontFamily="pack://application:,,,/Assets/Fonts/#HarmonyOS Sans SC"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="{Binding Percent, StringFormat={}({0:P1})}" />

            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="PopupTemplate" DataType="{x:Type viewModels:StatisticDataViewModel}">
            <StackPanel>
                <!--  Debug info with direct binding  -->
                <TextBlock
                    Margin="2"
                    FontSize="10"
                    Foreground="Yellow"
                    IsHitTestVisible="False"
                    Text="{Binding Player.Name, StringFormat='DEBUG: Player={0}'}"
                    Visibility="{Binding Debug.Enabled, Converter={StaticResource BoolToVisibilityConverter}}" />
                <!--  Show skill count  -->
                <TextBlock
                    Margin="2"
                    FontSize="10"
                    Foreground="Cyan"
                    IsHitTestVisible="False"
                    Text="{Binding SkillList.Count, StringFormat='Skills Count: {0}'}"
                    Visibility="{Binding Debug.Enabled, Converter={StaticResource BoolToVisibilityConverter}}" />
                <!--  Original popup content  -->
                <ctrl:DpsDetailPopup SkillList="{Binding SkillList}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="ProgressBarDataTemplate" DataType="{x:Type viewModels:StatisticDataViewModel}">
            <Grid Height="36" Margin="0">
                <Button
                    Margin="1"
                    Padding="0"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding ItemClickCommand, RelativeSource={RelativeSource AncestorType=ctrl:SortedDpsControl}}"
                    CommandParameter="{Binding}"
                    FocusVisualStyle="{x:Null}"
                    Focusable="False"
                    IsTabStop="False">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="OverridesDefaultStyle" Value="True" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Style>
                    </Button.Style>

                    <!--  simple ControlTemplate so ContentPresenter stretches to available size  -->
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>

                    <!--  actual visual content, will now stretch to fill the button  -->
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <ctrl:DpsIndicatorControl
                            Grid.Column="1"
                            Margin="1"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            IndicatorForeground="{Binding Player.Spec, Converter={StaticResource ClassSpecColorConverter}}"
                            OverlayContent="{Binding}"
                            OverlayTemplate="{StaticResource StatisticDetailDataTemplate}"
                            Percentage="{Binding PercentOfMax}"
                            PopupContent="{Binding}"
                            PopupTemplate="{StaticResource PopupTemplate}">
                            <ctrl:DpsIndicatorControl.TrackOpacity>
                                <MultiBinding Converter="{StaticResource ConditionalPercentToOpacityConverter}">
                                    <Binding Path="DataContext.AppConfig.Opacity" RelativeSource="{RelativeSource AncestorType=Window}" />
                                    <Binding Path="DataContext.AppConfig.MouseThroughEnabled" RelativeSource="{RelativeSource AncestorType=Window}" />
                                </MultiBinding>
                            </ctrl:DpsIndicatorControl.TrackOpacity>
                        </ctrl:DpsIndicatorControl>
                    </Grid>
                </Button>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <!--  Main Border: Apply theme color as base background (like MainView)  -->
    <Border
        Style="{StaticResource ShadowWindowBorder}"
        Background="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeColorConverter}, FallbackValue=#0047AB}">

        <Grid>
            <!--  Optional Background Image Layer (on top of theme color)  -->
            <Border
                CornerRadius="{StaticResource MainCornerRadius}"
                Panel.ZIndex="0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{x:Null}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding AppConfig.ShowBackgroundInDpsMeter}" Value="True">
                                <Setter Property="Background" Value="{Binding AppConfig.BackgroundImagePath, Converter={StaticResource BackgroundImageConverter}}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

            <!--  Theme Color Filter Overlay (using theme color for better visibility)  -->
            <Border
                CornerRadius="{StaticResource MainCornerRadius}"
                Panel.ZIndex="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{x:Null}" />
                        <Style.Triggers>
                            <!-- Show color filter when background image is active, using theme color -->
                            <DataTrigger Binding="{Binding AppConfig.ShowBackgroundInDpsMeter}" Value="True">
                                <Setter Property="Background" Value="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeColorOverlayConverter}, ConverterParameter=0.4}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

            <!--  Main Content Grid  -->
            <Grid CacheMode="BitmapCache" Panel.ZIndex="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="25" />
                </Grid.RowDefinitions>

            <Border
                Height="25"
                VerticalAlignment="Top"
                Panel.ZIndex="2"
                Background="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeBackgroundConverter}, ConverterParameter=dark}"
                BorderBrush="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeBackgroundConverter}, ConverterParameter=medium}"
                BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.CornerRadius>
                    <CornerRadius
                        BottomLeft="0"
                        BottomRight="0"
                        TopLeft="{StaticResource MainCornerRadiusDouble}"
                        TopRight="{StaticResource MainCornerRadiusDouble}" />
                </Border.CornerRadius>
                <Border.Effect>
                    <DropShadowEffect
                        Color="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeAccentConverter}}"
                        BlurRadius="8"
                        ShadowDepth="0"
                        Opacity="0.1" />
                </Border.Effect>
                <!--  顶部栏,Grid 一般不设置属性  -->
                <Grid Margin="2">
                    <!--  统计类型标注  -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  左侧按钮  -->
                    <Button
                        x:Name="LeftButton"
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        Command="{Binding PreviousMetricTypeCommand}"
                        Content="&#xe689;"
                        FontSize="15" />
                    <!--  当前统计状态文字  -->
                    <TextBlock Grid.Column="1" Style="{StaticResource StatisticTypeText}">
                        <Run Text="{lex:Loc {Binding StatisticIndex, StringFormat=StatisticType_{0}}}" />
                        <Run Text="(" />
                        <Run Text="{lex:Loc {Binding ScopeTime, StringFormat=ScopeTime_{0}}}" />
                        <Run Text=")" />
                    </TextBlock>
                    <!--  右侧按钮  -->
                    <Button
                        x:Name="RightButton"
                        Grid.Column="2"
                        VerticalAlignment="Center"
                        Command="{Binding NextMetricTypeCommand}"
                        Content="&#xe688;"
                        FontSize="15" />
                    <!--  切换按钮  -->
                    <Button
                        x:Name="SwitchButton"
                        Grid.Column="3"
                        Command="{Binding ToggleScopeTimeCommand}"
                        Content=""
                        FontSize="12"
                        ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Tooltip_ToggleScope}}"
                        ToolTipService.InitialShowDelay="200"
                        ToolTipService.Placement="Mouse"
                        ToolTipService.ShowDuration="3000" />

                    <!--  刷新按钮  -->
                    <StackPanel
                        Grid.Column="4"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">

                        <!--  Minimize window button  -->
                        <Button
                            Width="21"
                            Command="{Binding MinimizeWindowCommand}"
                            Content="—"
                            FontSize="12" />

                        <!--  Mouse-through indicator (visible when click-through is enabled)  -->
                        <TextBlock
                            Width="21"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="pack://application:,,,/Assets/Fonts/#IconFont"
                            FontSize="15"
                            Foreground="White"
                            Text="&#xe83a;"
                            TextAlignment="Center"
                            ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.DpsStatistics_Indicator_ClickThrough_Tooltip}}"
                            Visibility="{Binding DataContext.AppConfig.MouseThroughEnabled, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <!--  Topmost toggle button (pin / pin-fill)  -->
                        <Button
                            x:Name="TopmostButton"
                            Width="21"
                            Command="{Binding ToggleTopmostCommand}"
                            FontFamily="pack://application:,,,/Assets/Fonts/#iconfont"
                            FontSize="15"
                            ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.DpsStatistics_Indicator_WindowTopMost_Tooltip}}">
                            <Button.Style>
                                <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                                    <Setter Property="Content" Value="&#xe864;" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Topmost, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                            <Setter Property="Content" Value="&#xe863;" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <!--  刷新按钮  -->
                        <Button
                            Width="21"
                            Command="{Binding ResetAllCommand}"
                            Content="&#xE644;" />
                        <!--  设置按钮  -->
                        <Button Width="24" Content="&#xE600;">
                            <b:Interaction.Behaviors>
                                <behaviors:OpenContextMenuBehavior />
                            </b:Interaction.Behaviors>
                            <Button.ContextMenu>
                                <ContextMenu Style="{StaticResource ModernContextMenu}">
                                    <!--  只给 MenuItem 应用样式，Separator 不受影响  -->
                                    <ContextMenu.Resources>
                                        <Style BasedOn="{StaticResource DpsSettingsMenuItem}" TargetType="{x:Type MenuItem}" />
                                    </ContextMenu.Resources>
                                    <MenuItem
                                        Command="{Binding OpenSettingsCommand}"
                                        Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_Settings}}"
                                        IsCheckable="false" />
                                    <Separator />

                                    <MenuItem
                                        Command="{Binding ToggleScopeTimeCommand}"
                                        Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_ShowTotal}}"
                                        Style="{StaticResource DpsCheckableMenuItem}">
                                        <MenuItem.IsChecked>
                                            <MultiBinding Converter="{StaticResource EqualsConverter}" Mode="OneWay">
                                                <Binding Path="ScopeTime" />
                                                <Binding Source="{x:Static wpfModels:ScopeTime.Total}" />
                                            </MultiBinding>
                                        </MenuItem.IsChecked>
                                    </MenuItem>

                                    <!--  记录设置  -->
                                    <MenuItem Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_RecordSettings}}" ItemContainerStyle="{StaticResource CheckableMenuItem}">
                                        <!--  pass a parameter (literal) to the command  -->
                                        <MenuItem Command="{Binding Options.SetMinimalDurationCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_Record_Always}}">
                                            <MenuItem.CommandParameter>
                                                <system:Int32>-1</system:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                        <MenuItem
                                            Command="{Binding Options.SetMinimalDurationCommand}"
                                            Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_Record_Skip1Min}}"
                                            IsChecked="{Binding Options.MinimalDurationInSeconds}">
                                            <MenuItem.CommandParameter>
                                                <system:Int32>2</system:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                        <!--  Click="RecordSettingsMenuItem_Click"  -->
                                        <MenuItem
                                            Command="{Binding Options.SetMinimalDurationCommand}"
                                            Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_Record_Skip2Min}}"
                                            IsChecked="{Binding Options.MinimalDurationInSeconds}">
                                            <MenuItem.CommandParameter>
                                                <system:Int32>5</system:Int32>
                                            </MenuItem.CommandParameter>
                                        </MenuItem>
                                    </MenuItem>
                                    <!--  技能显示设置  -->
                                    <MenuItem Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_SkillDisplay}}" ItemContainerStyle="{StaticResource CheckableMenuItem}">
                                        <MenuItem Command="{Binding SetSkillDisplayLimitCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_SkillDisplay_Top5}}">
                                            <MenuItem.CommandParameter>
                                                <system:Int32>5</system:Int32>
                                            </MenuItem.CommandParameter>
                                            <MenuItem.IsChecked>
                                                <MultiBinding Converter="{StaticResource EqualsConverter}" Mode="OneWay">
                                                    <Binding Path="CurrentStatisticData.SkillDisplayLimit" />
                                                    <Binding Path="CommandParameter" RelativeSource="{RelativeSource Self}" />
                                                </MultiBinding>
                                            </MenuItem.IsChecked>
                                        </MenuItem>
                                        <MenuItem Command="{Binding SetSkillDisplayLimitCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_SkillDisplay_Top8}}">
                                            <MenuItem.CommandParameter>
                                                <system:Int32>8</system:Int32>
                                            </MenuItem.CommandParameter>
                                            <MenuItem.IsChecked>
                                                <MultiBinding Converter="{StaticResource EqualsConverter}" Mode="OneWay">
                                                    <Binding Path="CurrentStatisticData.SkillDisplayLimit" />
                                                    <Binding Path="CommandParameter" RelativeSource="{RelativeSource Self}" />
                                                </MultiBinding>
                                            </MenuItem.IsChecked>
                                        </MenuItem>
                                        <MenuItem Command="{Binding SetSkillDisplayLimitCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_SkillDisplay_Top10}}">
                                            <MenuItem.CommandParameter>
                                                <system:Int32>10</system:Int32>
                                            </MenuItem.CommandParameter>
                                            <MenuItem.IsChecked>
                                                <MultiBinding Converter="{StaticResource EqualsConverter}" Mode="OneWay">
                                                    <Binding Path="CurrentStatisticData.SkillDisplayLimit" />
                                                    <Binding Path="CommandParameter" RelativeSource="{RelativeSource Self}" />
                                                </MultiBinding>
                                            </MenuItem.IsChecked>
                                        </MenuItem>
                                        <MenuItem Command="{Binding SetSkillDisplayLimitCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_SkillDisplay_Top15}}">
                                            <MenuItem.CommandParameter>
                                                <system:Int32>15</system:Int32>
                                            </MenuItem.CommandParameter>
                                            <MenuItem.IsChecked>
                                                <MultiBinding Converter="{StaticResource EqualsConverter}" Mode="OneWay">
                                                    <Binding Path="CurrentStatisticData.SkillDisplayLimit" />
                                                    <Binding Path="CommandParameter" RelativeSource="{RelativeSource Self}" />
                                                </MultiBinding>
                                            </MenuItem.IsChecked>
                                        </MenuItem>
                                        <MenuItem Command="{Binding SetSkillDisplayLimitCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_SkillDisplay_All}}">
                                            <MenuItem.CommandParameter>
                                                <system:Int32>0</system:Int32>
                                            </MenuItem.CommandParameter>
                                            <MenuItem.IsChecked>
                                                <MultiBinding Converter="{StaticResource EqualsConverter}" Mode="OneWay">
                                                    <Binding Path="CurrentStatisticData.SkillDisplayLimit" />
                                                    <Binding Path="CommandParameter" RelativeSource="{RelativeSource Self}" />
                                                </MultiBinding>
                                            </MenuItem.IsChecked>
                                        </MenuItem>
                                    </MenuItem>
                                    <!--  历史记录  -->
                                    <MenuItem Command="{Binding OpenEncounterHistoryCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_History}}" ItemContainerStyle="{StaticResource CheckableMenuItem}" />
                                    <!--  打桩模式  -->
                                    <MenuItem
                                        Click="PilingMenuItem_Click"
                                        Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_Training_Personal}}"
                                        IsCheckable="True"
                                        Style="{StaticResource CheckableMenuItem}" />

                                    <MenuItem Command="{Binding OpenSkillDiaryCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_SkillDiary}}" />

                                    <Separator />
                                    <MenuItem Command="{Binding ShutdownCommand}" Header="{lex:Loc Key={x:Static loc:ResourcesKeys.Menu_Exit}}" />
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                        <!--  隐藏按钮  -->
                        <Button
                            x:Name="PullButton"
                            Width="22"
                            Click="PullButton_Click"
                            Content="&#xE601;"
                            RenderTransformOrigin="0.5,0.5">
                            <Button.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform />
                                    <SkewTransform />
                                    <RotateTransform Angle="0" />
                                    <TranslateTransform />
                                </TransformGroup>
                            </Button.RenderTransform>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="Transparent">
                <ctrl:SortedDpsControl
                    ItemTemplate="{StaticResource ProgressBarDataTemplate}"
                    ItemsSource="{Binding CurrentStatisticData.Data}"
                    SelectedItem="{Binding CurrentStatisticData.SelectedSlot}"
                    ItemClickCommand="{Binding OpenPlayerSkillBreakdownCommand}" />
            </Border>

            <Border
                x:Name="LoadingGridHost"
                Grid.Row="2"
                Height="5"
                Background="Transparent"
                ClipToBounds="True">

                <!--  TODO: 当 DataStorage 触发 ServerConnectionStateChanged 时, 隐藏该 Grid  -->
                <Rectangle x:Name="LoadingStripe" Style="{StaticResource LoadingBlockRectangle}">
                    <Rectangle.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation
                                        AccelerationRatio="0.2"
                                        DecelerationRatio="0.2"
                                        Storyboard.TargetName="LoadingStripe"
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                        Duration="0:0:1.6">
                                        <DoubleAnimation.From>
                                            <Binding
                                                Converter="{StaticResource DoubleNegateConverter}"
                                                ElementName="LoadingStripe"
                                                Path="ActualWidth" />
                                        </DoubleAnimation.From>
                                        <DoubleAnimation.To>
                                            <MultiBinding Converter="{StaticResource DoubleSumConverter}">
                                                <Binding ElementName="LoadingGridHost" Path="ActualWidth" />
                                                <Binding ElementName="LoadingStripe" Path="ActualWidth" />
                                            </MultiBinding>
                                        </DoubleAnimation.To>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Rectangle.Triggers>
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1, 0">
                            <GradientStop Offset="0" Color="#0000D9FF" />
                            <GradientStop Offset="0.4" Color="#FF00D9FF" />
                            <GradientStop Offset="0.6" Color="#FFA855F7" />
                            <GradientStop Offset="1" Color="#00A855F7" />
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
            </Border>

            <Border
                x:Name="ContentBorder"
                Grid.Row="3"
                VerticalAlignment="Bottom"
                Height="25"
                Opacity="1"
                Background="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeBackgroundConverter}, ConverterParameter=dark}"
                BorderBrush="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeBackgroundConverter}, ConverterParameter=medium}"
                BorderThickness="0,1,0,0"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.CornerRadius>
                    <CornerRadius
                        BottomLeft="{StaticResource MainCornerRadiusDouble}"
                        BottomRight="{StaticResource MainCornerRadiusDouble}"
                        TopLeft="0"
                        TopRight="0" />
                </Border.CornerRadius>
                <Border.Effect>
                    <DropShadowEffect
                        Color="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeAccentConverter}}"
                        BlurRadius="8"
                        ShadowDepth="0"
                        Opacity="0.1" />
                </Border.Effect>
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <Binding
                                        Converter="{StaticResource LessThanToBoolConverter}"
                                        ConverterParameter="60"
                                        Path="ActualHeight"
                                        RelativeSource="{RelativeSource AncestorType=local:DpsStatisticsView}" />
                                </DataTrigger.Binding>
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="Opacity"
                                                To="0"
                                                Duration="0:0:0.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="Opacity"
                                                To="1"
                                                Duration="0:0:0.2" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid Margin="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Normal Mode Display - Current Battle -->
                    <TextBlock
                        Grid.Column="0"
                        HorizontalAlignment="Left"
                        Text="{Binding CurrentStatisticData.CurrentPlayerSlot.Index, StringFormat=[{0}], TargetNullValue=[--], FallbackValue=[--]}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource FooterTextContent}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHistoryMode}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsShowingLastBattle}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Normal Mode Display - Last Battle Indicator -->
                    <TextBlock
                        Grid.Column="0"
                        HorizontalAlignment="Left"
                        Text="[LAST]"
                        Foreground="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeAccentConverter}}"
                        FontWeight="Bold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource FooterTextContent}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsHistoryMode}" Value="False" />
                                            <Condition Binding="{Binding IsShowingLastBattle}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock
                        Grid.Column="1"
                        Margin="6,0,0,0"
                        HorizontalAlignment="Left"
                        Style="{StaticResource FooterTextContent}"
                        Text="{Binding Path=BattleDuration, StringFormat={}{0:mm\\:ss}}"
                        Visibility="{Binding IsHistoryMode, Converter={StaticResource BoolToInvertedVisibilityConverter}}" />

                    <!-- History Mode Display -->
                    <Button
                        Grid.Column="0"
                        HorizontalAlignment="Left"
                        Command="{Binding ReturnToLiveCommand}"
                        Content="[HISTORY]"
                        Foreground="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeAccentConverter}}"
                        Background="Transparent"
                        BorderThickness="0"
                        FontWeight="Bold"
                        Cursor="Hand"
                        ToolTip="Click to return to live mode"
                        Visibility="{Binding IsHistoryMode, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <TextBlock
                                                Text="{TemplateBinding Content}"
                                                Foreground="{TemplateBinding Foreground}"
                                                FontWeight="{TemplateBinding FontWeight}"
                                                Cursor="{TemplateBinding Cursor}" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <TextBlock
                        Grid.Column="1"
                        Margin="6,0,0,0"
                        HorizontalAlignment="Left"
                        Style="{StaticResource FooterTextContent}"
                        Text="{Binding HistoryModeLabel}"
                        Foreground="{Binding AppConfig.ThemeColor, Converter={StaticResource ThemeAccentConverter}}"
                        Visibility="{Binding IsHistoryMode, Converter={StaticResource BoolToVisibilityConverter}}" />

                    <TextBlock
                        Grid.Column="2"
                        Margin="0,0,4,0"
                        HorizontalAlignment="Right"
                        Style="{StaticResource FooterTextContent}"
                        TextAlignment="Right">
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource HumanReadableNumberConverter}">
                                <Binding
                                    FallbackValue="--"
                                    Path="CurrentStatisticData.CurrentPlayerSlot.Value"
                                    TargetNullValue="--" />
                                <Binding Path="DataContext.AppConfig.DamageDisplayType" RelativeSource="{RelativeSource AncestorType=Window, Mode=FindAncestor}" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Grid>
            </Border>

            </Grid> <!-- End of Content Grid with CacheMode -->
        </Grid> <!-- End of Layering Grid -->
    </Border> <!-- End of Main Border with Theme Color -->


</Window>
