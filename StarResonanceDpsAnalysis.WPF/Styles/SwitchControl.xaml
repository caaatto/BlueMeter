<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:StarResonanceDpsAnalysis.WPF.Controls">

    <Style x:Key="TrackBorder" TargetType="Border">
        <Setter Property="CornerRadius" Value="{Binding RelativeSource={RelativeSource Self}, Path=ActualHeight, Converter={StaticResource HalfValueConverter}}" />
        <Setter Property="Height" Value="{Binding Height, RelativeSource={RelativeSource TemplatedParent}}" />
        <Setter Property="Width">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource SwitchTrackWidthConverter}">
                    <Binding ElementName="PART_OnLabel" Path="ActualWidth" />
                    <Binding ElementName="PART_OffLabel" Path="ActualWidth" />
                    <Binding Path="ActualHeight" RelativeSource="{RelativeSource Self}" />
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ThumbBorder" TargetType="Border">
        <Setter Property="Margin" Value="3" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="Background" Value="#FFF" />
        <Setter Property="BorderBrush" Value="#CCC" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="ContentBorderBase" TargetType="Border">
        <Setter Property="Width">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource MinusConverter}">
                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType={x:Type Border}}" />
                    <Binding Path="ActualHeight" RelativeSource="{RelativeSource AncestorType={x:Type Border}}" />
                    <Binding Source="-3" />
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="OnContentBorder"
        BasedOn="{StaticResource ContentBorderBase}"
        TargetType="Border">
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform X="0" Y="0" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="OffContentBorder"
        BasedOn="{StaticResource ContentBorderBase}"
        TargetType="Border">
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform X="0" Y="0" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ContentBase" TargetType="ContentControl">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="FontSize" Value="10" />
    </Style>

    <Style
        x:Key="OnContent"
        BasedOn="{StaticResource ContentBase}"
        TargetType="ContentControl">
        <Setter Property="Foreground" Value="#FFF" />
        <Setter Property="Opacity" Value="0" />
    </Style>

    <Style
        x:Key="OffContent"
        BasedOn="{StaticResource ContentBase}"
        TargetType="ContentControl">
        <Setter Property="Foreground" Value="#000" />
        <Setter Property="Opacity" Value="1" />
    </Style>

    <Style TargetType="{x:Type controls:SwitchControl}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SwitchControl}">
                    <Border x:Name="PART_Track" Style="{StaticResource TrackBorder}">
                        <Border.Background>
                            <SolidColorBrush x:Name="TrackBrush" Color="{StaticResource AntBorderColor}" />
                        </Border.Background>
                        <Grid>
                            <Border x:Name="PART_OnBorder" Style="{StaticResource OnContentBorder}">
                                <ContentControl
                                    x:Name="PART_OnLabel"
                                    Content="{TemplateBinding OnContent}"
                                    FontSize="{TemplateBinding FontSize}"
                                    Style="{StaticResource OnContent}" />
                            </Border>

                            <Border x:Name="PART_OffBorder" Style="{StaticResource OffContentBorder}">
                                <ContentControl
                                    x:Name="PART_OffLabel"
                                    Content="{TemplateBinding OffContent}"
                                    FontSize="{TemplateBinding FontSize}"
                                    Style="{StaticResource OffContent}" />
                            </Border>

                            <Border
                                x:Name="PART_Thumb"
                                Width="{Binding RelativeSource={RelativeSource Self}, Path=ActualHeight}"
                                MinHeight="12"
                                VerticalAlignment="Stretch"
                                CornerRadius="{Binding RelativeSource={RelativeSource Self}, Path=ActualHeight, Converter={StaticResource HalfValueConverter}}"
                                RenderTransformOrigin="0.5,0.5"
                                Style="{StaticResource ThumbBorder}">
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="ThumbTranslate" X="0" />
                                </Border.RenderTransform>
                            </Border>
                        </Grid>

                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  Animate when checked  -->
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="TrackBrush"
                                            Storyboard.TargetProperty="Color"
                                            To="{StaticResource AntPrimaryColor}"
                                            Duration="0:0:0.2" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="PART_OnLabel"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0:0:0.15" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="PART_OffLabel"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="TrackBrush"
                                            Storyboard.TargetProperty="Color"
                                            To="{StaticResource AntBorderColor}"
                                            Duration="0:0:0.2" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="PART_OnLabel"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0"
                                            Duration="0:0:0.15" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="PART_OffLabel"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_Track" Property="Opacity" Value="0.5" />
                            <Setter TargetName="PART_Thumb" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
