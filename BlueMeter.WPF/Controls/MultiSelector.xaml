<UserControl x:Class="BlueMeter.WPF.Controls.MultiSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BlueMeter.WPF.Controls"
             xmlns:c="clr-namespace:BlueMeter.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="200">
    <UserControl.Resources>
        <Style x:Key="MultiSelectorBorder" TargetType="Border">
            <Setter Property="Background" Value="#DFDFDF" />
            <Setter Property="Padding" Value="2,2,2,2" />
            <Setter Property="CornerRadius">
                <Setter.Value>
                    <Binding Path="ActualHeight"
                             RelativeSource="{RelativeSource Self}">
                        <Binding.Converter>
                            <c:HalfValueConverter />
                        </Binding.Converter>
                    </Binding>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SelectionButton" TargetType="Border">
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="CornerRadius">
                <Setter.Value>
                    <Binding Path="ActualHeight"
                             RelativeSource="{RelativeSource Self}">
                        <Binding.Converter>
                            <c:HalfValueConverter />
                        </Binding.Converter>
                    </Binding>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SelectionButtonActive" TargetType="Border" BasedOn="{StaticResource SelectionButton}">
            <Setter Property="Background" Value="#FFF" />
        </Style>

        <Style TargetType="{x:Type local:MultiSelectorItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type local:MultiSelectorItem}">
                        <Border x:Name="ItemsBorder" Style="{StaticResource SelectionButton}">
                            <ContentPresenter ContentSource="Content" />
                        </Border>

                        <!-- TODO Here -->

                        <ControlTemplate.Triggers>
                            <!-- 用 MultiBinding 比较 AlternationIndex 和 ActiveIndex 是否相等 -->
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <MultiBinding Converter="{StaticResource EqualsConverter}">
                                        <!-- 模板宿主(容器)上的 AlternationIndex -->
                                        <Binding Path="(ItemsControl.AlternationIndex)"
                                                 RelativeSource="{RelativeSource TemplatedParent}" />
                                        <!-- 往上找 MultiSelector 的 ActiveIndex -->
                                        <Binding Path="ActiveIndex"
                                                 RelativeSource="{RelativeSource AncestorType={x:Type local:MultiSelector}}" />
                                    </MultiBinding>
                                </DataTrigger.Binding>
                                <Setter TargetName="ItemsBorder"
                                        Property="Style"
                                        Value="{StaticResource SelectionButtonActive}" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource MultiSelectorBorder}">
        <!--测试用样式-->
        <!--
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource SelectionButtonActive}" Grid.Column="0">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="123"/>
            </Border>
            <Border Style="{StaticResource SelectionButton}" Grid.Column="1">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="456"/>
            </Border>
            <Border Style="{StaticResource SelectionButton}" Grid.Column="2">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="789"/>
            </Border>
        </Grid>
        -->

        <local:MultiSelectorItemsHost x:Name="SelectionItems"
                                      AlternationCount="10000">

            <local:MultiSelectorItemsHost.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1" />
                </ItemsPanelTemplate>
            </local:MultiSelectorItemsHost.ItemsPanel>
        </local:MultiSelectorItemsHost>
    </Border>

</UserControl>